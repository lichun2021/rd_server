## Increase Gradle daemon heap to avoid OOM during Protocol/Protobuf build
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+UseG1GC -Dfile.encoding=UTF-8
org.gradle.daemon=true
## Keep concurrency low to reduce memory pressure on Windows
org.gradle.workers.max=1
org.gradle.parallel=false

